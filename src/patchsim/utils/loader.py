"""Deprecated placeholder module.

Legacy loader helpers were removed because they were unused and did not reflect
the active simulation pipeline. Use `patchsim.core.simulation` and
`patchsim.utils.logger` / `patchsim.utils.viz` instead.
"""
